For this to properly work please open the index.html file in vscode and then start a local server by clicking on "GoLive" 

This is because browsers by default do not allow crossorigin file access when running through path

Thanks!